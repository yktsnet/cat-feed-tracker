# Copy this file to secrets.py and fill in real values.
# secrets.py is stored on the Pico W only — never commit real values.

WIFI_SSID     = "YOUR_SSID"
WIFI_PASSWORD = "YOUR_PASSWORD"
SERVER_URL    = "https://your-domain.example.com/api/events"
DEVICE_TOKEN  = "your-device-token"
